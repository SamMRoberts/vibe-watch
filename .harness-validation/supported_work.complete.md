# Supported Work

Complete.

- Scaffold a new Go module for a CLI-only, TUI-only, or hybrid CLI/TUI app.
- Add Cobra command trees, flags, validation, completion, help output, and scriptable command behavior.
- Add Bubble Tea models, Bubbles components, Lip Gloss styling, key bindings, and terminal layouts.
- Keep domain behavior in testable packages outside `cmd/` and outside Bubble Tea models.
- Add focused tests and docs for commands, domain logic, model transitions, layout helpers, and terminal behavior.
