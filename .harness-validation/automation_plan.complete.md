# Automation Plan

Complete.

- Keep the harness in `AGENTS.md` for automatic discovery by future agents.
- Keep `.harness-validation/` section files for completeness auditing.
- Add scripts only after repeated manual checks become stable enough to automate.
- Add a companion skill only if this repo develops reusable CLI/TUI conventions beyond this project.
- Do not add hooks or MCP servers unless persistent memory, enforcement, or cross-repo reuse is requested.
